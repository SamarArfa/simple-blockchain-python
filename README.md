# simple-blockchain-python


A secure, educational blockchain system built in Python that demonstrates core concepts of decentralized ledgers, including digital signatures, proof of work, transaction validation, and tamper detection.



## Requirements

- Python 3.7+
- `cryptography` library

Install dependencies:
```bash
pip install cryptography
